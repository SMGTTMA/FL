请在所有回复中遵循以下指令（如果与我的查询相关）：

# 语言要求
- 请使用中文回复
- 接口设计不用写swagger注解

# 项目开发规范

## 代码风格
- 使用 TypeScript 进行开发
- 使用 ESLint + Prettier 进行代码格式化
- 使用 2 个空格进行缩进
- 使用单引号作为字符串引号
- 行尾使用分号
- 使用驼峰命名法（camelCase）命名变量和函数
- 使用帕斯卡命名法（PascalCase）命名类和接口
- 使用下划线命名法（snake_case）命名数据库字段

## API 开发规范
- 所有 API 请求使用 POST 方法
- 返回给前端的 JSON 数据使用驼峰命名
- API 响应统一格式：
  ```typescript
  {
    code: number;    // 状态码
    message: string; // 提示信息
    data: any;       // 数据
  }
  ```
- 使用 class-validator 进行请求参数验证
- 使用 class-transformer 进行数据转换

## 目录结构规范
- 使用 NestJS 推荐的模块化结构
- 每个功能模块包含以下文件：
  - module.ts
  - controller.ts
  - service.ts
  - dto/
  - entities/
  - interfaces/

## 数据库规范
- 使用 TypeORM 进行数据库操作
- 数据库时间字段统一使用 datetime 格式
- 表名使用复数形式
- 主键统一使用 id
- 创建时间和更新时间字段统一使用 createdAt 和 updatedAt

## 安全规范
- 敏感信息（如密钥、密码等）必须使用环境变量
- 使用 JWT 进行身份认证
- 密码必须加密存储
- API 调用必须做好错误处理
- 使用参数验证防止 SQL 注入

## 依赖管理
- 使用 pnpm 作为包管理器
- 使用绝对路径引入模块

## Git 提交规范
提交信息格式：`type(scope): subject`

type 类型：
- feat: 新功能
- fix: 修复
- docs: 文档
- style: 格式（不影响代码运行的变动）
- refactor: 重构
- test: 增加测试
- chore: 构建过程或辅助工具的变动

scope 范围：
- api: API 相关
- db: 数据库相关
- auth: 认证相关
- config: 配置相关
- utils: 工具函数
- test: 测试相关

## 注释规范
- 关键代码和复杂逻辑必须添加注释
- 使用 JSDoc 格式编写函数注释
- 复杂业务逻辑需要添加详细说明

以上规范请在开发过程中严格遵守。如果有特殊情况需要违反规范，请在代码中添加相应的说明注释。